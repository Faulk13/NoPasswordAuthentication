version: 2
jobs:
    build:
        working_directory: '~/workspace'
        docker:
            - image: 'circleci/php:7.2'
        steps:
            - checkout
            - run:
                name: 'Install Composer Dependencies'
                command: |
                    composer install

